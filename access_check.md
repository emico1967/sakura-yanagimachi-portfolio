# 公開URL検証記録

## GitHub Pages公開確認

GitHub Pagesは `gh-pages` ブランチを配信元として有効であり、公開状態が `built` であることを確認した。静的アセットは、リポジトリ配下のベースパスで読み込めるよう修正済みである。

ブラウザ表示の再検証では、サイトがGitHub Pagesのサブパスで実行されるため、クライアント側ルーターがルートを認識できず404画面を表示することが判明した。ルーターのベースパス設定を追加して再公開する。
